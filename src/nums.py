import math as m #Import math library and use it as 'm'.
import random as r
n = 10;
print n;
"""del(n); # Deletes object's reference.
print n;""" # compilation error, because it's undefined after calling 'del'

# Demonstrate some functions

print abs(n); 
print pow(n, 2); 
print m.sqrt(n);
print "random calling: ", r.random();